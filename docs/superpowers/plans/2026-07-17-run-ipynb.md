# run.ipynb Implementation Plan

> **For agentic workers:** Create `run.ipynb` per `docs/superpowers/specs/2026-07-17-run-ipynb-design.md`. Do not execute the full pipeline on WSL.

**Goal:** Root notebook that runs `Pipeline_Orchestrator().run(force=True)` like the CLI.

**Files:** create `run.ipynb` only; optional one-line pointer in `RUN.md`.

## Task 1: Create `run.ipynb`

- Cells 0–5 as in the design spec (markdown / setup / data check / knobs / run / next steps).
- Use `EditNotebook` (or notebook tools); do not hand-edit raw JSON.
- Verify imports resolve under `.venv` without starting `orchestrator.run()`.

# GPU-Fuzzy Trading Pipeline — Python 3.10+
# Install from project root:
#   pip install -r requirements.txt
#
# JAX note: for NVIDIA GPU, install jax/jaxlib per https://jax.readthedocs.io/en/latest/installation.html
# instead of the generic jaxlib wheels below.

# --- Core (required) ---
pandas>=2.0
# NumPy 2.4+ manylinux wheels require x86-64-v2 (SSE4.2+). Older VPS/VM CPUs need <2.4.
numpy>=1.26,<2.4
numba>=0.58
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0

# --- Phase 2: GPU backtest + evolutionary search (CPU fallbacks if omitted) ---
jax>=0.4.20
jaxlib>=0.4.20
evox>=0.9.0

# --- Phase 4: walk-forward risk optimization (required) ---
optuna>=3.5.0

# --- Development / testing ---
pytest>=7.4.0
hypothesis>=6.90.0

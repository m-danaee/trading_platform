# GPU-Fuzzy Trading Pipeline — Python 3.10+
# Install from project root:
#   pip install -r requirements.txt
#
# JAX: generic CPU wheels below. For Phase 2 GPU backtests on NVIDIA hardware,
# install a matching CUDA build AFTER this file (plugin versions must align):
#   pip install -r requirements-gpu.txt
# Colab T4:  pip install -U "jax[cuda12]==0.10.1"
# Local WSL/Linux (CUDA 13 / RTX 3080 Ti): pip install -r requirements-gpu.txt
# See https://jax.readthedocs.io/en/latest/installation.html

# --- Core (required) ---
pandas>=2.0
# NumPy 2.4+ manylinux wheels require x86-64-v2 (SSE4.2+). Older VPS/VM CPUs need <2.4.
numpy>=1.26,<2.4
numba>=0.58
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0

# --- Phase 2: GPU backtest + evolutionary search (CPU fallbacks if omitted) ---
jax>=0.4.20
jaxlib>=0.4.20
evox>=1.0.0,<2
torch>=2.0

# --- RB validation-only hyperparameter search (required) ---
optuna>=3.5.0

# --- Development / testing ---
pytest>=7.4.0
hypothesis>=6.90.0

# GPU-Fuzzy Trading Pipeline — Python 3.10+
# Install from project root:
#   pip install -r requirements.txt
#
# JAX note: for NVIDIA GPU, install jax/jaxlib per https://jax.readthedocs.io/en/latest/installation.html
# instead of the generic jaxlib wheels below.

# --- Core (required) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0

# --- Phase 2: GPU backtest + evolutionary search (CPU fallbacks if omitted) ---
jax>=0.4.20
jaxlib>=0.4.20
evox>=0.9.0

# --- Phase 4: RL risk optimization (random-search fallback if omitted) ---
stable-baselines3>=2.2.0
gymnasium>=0.29.0
torch>=2.0.0
optuna>=3.5.0

# --- Development / testing ---
pytest>=7.4.0
hypothesis>=6.90.0

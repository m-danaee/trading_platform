# Implementation Plans

Generated by improve on 2026-07-16 against commit `425f469`.

## Execution order & status

| Plan | Title                                                          | Priority | Effort | Depends on | Status |
| ---- | -------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 002  | RB fail-closed on concentration and tail-holdout gates         | P1       | M      | —          | TODO   |
| 003  | Align Phase2 fitness with deployable via val penalties         | P1       | M      | —          | TODO   |
| 004  | Re-enable Phase1 top-K and multi-symbol islands with migration | P2       | M      | 003        | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- **002** and **003** can run in parallel.
- **004** depends on **003** conceptually: enabling Phase1 + multi-symbol islands without fitness/val steering recreates train-only specialists that fail RB concentration. Execute 003 before 004.
- Do **not** run the full pipeline on WSL (OOM). Colab GPU for end-to-end validation. Local: `PYTEST_LOW_MEMORY=1` and only related unit tests.
- Do **not** modify `evaluator_v5.ipynb`.
- Every executor must invoke **codelookup** before editing source.

## Findings considered and rejected

| Finding                                               | Why rejected / deferred                                                                                                                                                                             |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Flip `PHASE2_JOINT_TRAIN_VAL=True` alone              | Config documents val double-counting leak (fitness + pool admission on same holdout). Prefer `PHASE2_VAL_IN_FITNESS_PENALTY=True` (plan 003) which steers overfit gap without joint Sortino/return. |
| Skip Phase5 entirely when `deployment_accepted=False` | Empty fail-closed strategies (plan 002) already neutralize OOS; Phase5-always-runs is a documented pipeline invariant. Prefer emptying rules over skipping Phase5.                                  |
| Raise pop/gens aggressively (200/50)                  | Memory/time risk on Colab; address search-space first via Phase1 top-K + clustered islands (004), then retune budget later if needed.                                                               |
| Loosen RB concentration thresholds                    | Would mask specialists from one-symbol islands; fix search architecture (004) instead of relaxing the gate.                                                                                         |
| Monthly admission full-restore removal alone          | Secondary to thin pools from train-only fitness; address fitness (003) first.                                                                                                                       |

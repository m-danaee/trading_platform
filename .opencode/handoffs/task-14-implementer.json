{
  "task": "task-14",
  "status": "DONE",
  "branch": "fix/rb-governor-rebalance",
  "changes": {
    "gpu_fuzzy_trader/config.py": [
      "C2: RB_REQUIRE_TRAIN_SLIGHTLY_ABOVE_VALID: True → False",
      "C3: RB_TRAIN_VALID_RETURN_GAP_WEIGHT: 0.25 → 4.0",
      "C3: RB_TRAIN_VALID_RATIO_GAP_WEIGHT: 6.0 → 30.0",
      "(included pre-existing task-8 tuning: MIN_TRADE_SUPPORT 75→120, MIN_TRADE_POOL_FLOOR 25→40, PHASE2_ORPHAN_MIN_TRADE_SUPPORT 15→20)"
    ],
    "gpu_fuzzy_trader/rb_governor.py": [
      "C1: _score_metrics weight rebalance 120/45→60/60 (ratio), 4.5/1.2→3.0/3.0 (return)",
      "C4: _score_metrics accepts cv_fold_returns kwarg, adds +8.0*min(fold_returns) − 3.0*std(fold_returns)",
      "C4: run_rb_governor_pipeline accepts cv_folds param, builds per-fold engines, passes fold returns to _score_metrics",
      "C4: _filter_good_rules accepts fold_engines param, computes CV fold returns per candidate",
      "M7: _combined_return_score accepts prev_pf/prev_dd, penalizes PF degradation (k=2.0) and DD increase (k=3.0)",
      "M7: All _combined_return_score callers updated to pass prev_pf/prev_dd where applicable"
    ]
  },
  "tests": {
    "PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest tests/unit/ -x -q -k \"rb_governor or scoring\"": "11 passed, 1201 deselected",
    "PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest tests/unit/ -x -q -k \"not phase2_rule_pool\"": "631 passed (pre-existing failures unrelated)"
  },
  "verified_config": {
    "RB_REQUIRE_TRAIN_SLIGHTLY_ABOVE_VALID": false,
    "RB_TRAIN_VALID_RETURN_GAP_WEIGHT": 4.0,
    "RB_TRAIN_VALID_RATIO_GAP_WEIGHT": 30.0
  },
  "notes": [
    "CV-fold consistency (C4) uses fold_engines built from cv_folds passed to run_rb_governor_pipeline.",
    "CvFoldValEvaluator not directly used because rb_governor works with rule dicts, not chromosomes. Instead per-fold CPUBacktestEngines are built and rule is simulated on each fold's valid_df.",
    "Fold evaluation only affects candidate singleton scoring (_filter_good_rules and fallback path) — not composition (would be too expensive).",
    "_combined_return_score prev_pf/prev_dd passed only in return_only mode paths where comparison state is tracked.",
    "CV-fold coefficient getattr defaults updated in _score_metrics to match new config values (30.0, 4.0)."
  ]
}

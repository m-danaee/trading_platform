{
  "task": "task-18",
  "status": "DONE",
  "commit": "aa34636",
  "branch": "fix/admission-gates-robustness",
  "files_changed": [
    "gpu_fuzzy_trader/backtest/cpu_engine.py",
    "gpu_fuzzy_trader/config.py",
    "gpu_fuzzy_trader/evolution/evox_runner.py",
    "gpu_fuzzy_trader/phases/phase2_rule_pool.py",
    "tests/unit/test_phase2_monthly_admission.py",
    ".opencode/tasks/task-18.md",
    ".opencode/CONTEXT.md"
  ],
  "tests": {
    "PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest tests/unit/ -x -q -k \"monthly or rule_pool or cpu_engine or cache\"": "243 passed, 1012 deselected (1 pre-existing failure deselected: test_evaluate_chromosome_diversity_penalty_avoids_self_penalization)"
  },
  "acceptance_criteria": {
    "M1": "Per-symbol win_rate is non-zero. sym_wins now tracked through _release_due_positions and used in else branch (non-log). Log-based path was already correct.",
    "M2": "Investigation written to task-18.md. Split boundaries documented: train Jan-Jun 2024, val Jun-Aug 2024, test Sep-Oct 2024. Regime flip identified (bullish→bearish, low-vol→high-vol around mid-June). Recommendation: keep current split + rely on CV-fold consistency from task-14/C4.",
    "M6": "PHASE2_EVAL_GLOBAL_CACHE_MAX_SIZE=575 in config.py, used in _store_global_metrics_cache and logged in _evaluate_population_indices.",
    "M8": "PHASE2_MONTHLY_ADMISSION_MIN_RATIO=0.667 in config.py, used in _apply_monthly_admission_gate. Tests updated to match new threshold (0.5→0.667). Log shows min_ratio=0.667."
  },
  "notes_for_reviewer": [
    "M1: The per-symbol win_rate bug was in the non-log path (return_logs=False). The log-based path (return_logs=True) already computed win_rate correctly from logs_df. Added sym_wins tracking to _release_due_positions and used it in the else branch.",
    "M2: The regime shift is a real market structure change (bullish→bearish around Jun 2024), not a split artifact. Recommendation is to keep current split and rely on task-14/C4 CV-fold consistency mitigation.",
    "M6: Added cache size logging at two points in _evaluate_population_indices (cache-hit-only path and GPU batch path) for visibility at gen 0.",
    "M8: Tests were updated to match min_ratio=0.667. Pre-existing test failure (test_evaluate_chromosome_diversity_penalty_avoids_self_penalization) is unrelated to this task.",
    "evaluator_v5.ipynb was NOT modified.",
    "No changes to data/splitter.py (M2 was investigation only)."
  ]
}

# Plan: fix-fold-gates — 2026-08-25
> Generated by writing-plans against commit 3d0ce31 (3d0ce312bd043594180fd82b7f3a106e264ca3c1) on branch main at 2026-08-25T16:30:00+03:30
> Drift check: executor must run `git rev-parse HEAD` and compare; if > 50 commits or base_branch changed, STOP and ask for re-plan/reconcile.
> Verification baseline (detected from repo):
> - build: none detected
> - test: PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q --ignore=tests/benchmark
> - lint: none detected
> - typecheck: none detected

## Goal
Fix the 5 verdict issues blocking approval of the unified-fold-gates series so the new fold-support ratio is actually used, count-gate scaling is active in Phase 2 with ceiling rounding, no runtime `rolling_cv` import remains, old `SPLIT_MODE` tests are updated, and low-priority cleanup (is_seed, inline tests, _run_nested_validation) is complete.

## Non-goals
- Retraining or changing hyperparameters/trading logic beyond wiring the new ratio/gate APIs
- Full CUDA / forward-OOS acceptance (out of scope for this fix run; focused low-memory tests only)
- Re-introducing the deleted `purged_walk_forward` or `SPLIT_MODE` abstractions

## Context & Evidence
- Decisions & trade-offs: keep the ratio-based support gate and ceiling-based count scaling as the single source of truth; prefer deprecating old names over re-adding them; keep Task-1 tests but make them assert production code
- Key files touched (with file:line evidence):
  - `gpu_fuzzy_trader/config.py:2225` — `required_folds(eligible, ratio)` correctly computes `max(2, ceil(eligible*ratio))` but is unused by discovery
  - `gpu_fuzzy_trader/mtf/discovery.py:645-646` — `min_fold_support = max(1, min(int(getattr(_cfg, "MTF_MIN_FOLD_SUPPORT", 2)), len(valid_folds)))` still uses removed fallback of 2, admitting rules with too little OOF support (High)
  - `gpu_fuzzy_trader/validation/fold_gates.py:238` — `build_fold_gate_context` has no production caller; `config.py:2250` returns `int(base)` when no reference given, and `phases/phase2_support.py:78` uses that path, so count-gate scaling is inactive; probe 25k rows still returns floors 120/25 unchanged; legacy rounding yields 11 where ceil yields 12 (High)
  - `gpu_fuzzy_trader/rb_governor.py:3403` — `from gpu_fuzzy_trader.validation.rolling_cv import cv_folds_only` runtime import remains after `rolling_cv.py` deletion (Medium)
  - `tests/unit/test_rolling_cv.py:11` — `from gpu_fuzzy_trader.validation.rolling_cv import …` fails at collection with ModuleNotFoundError (Medium)
  - `tests/property/test_data_splitter_properties.py:114` — `original_mode = config_mod.SPLIT_MODE` + `config_mod.SPLIT_MODE = "holdout"` AttributeError after SPLIT_MODE removal (Medium)
  - `tests/unit/test_phase2_support.py:343` — `monkeypatch.setattr(_cfg, "SPLIT_MODE", "purged_walk_forward")` AttributeError (Medium)
  - `gpu_fuzzy_trader/mtf/cross_fitting.py:49` — `is_seed` compatibility property remains; `tests/unit/test_unified_folds.py` etc still assert inline formulas rather than production `fold_gates`/`required_folds` (Low)
  - `gpu_fuzzy_trader/validation/nested_walk_forward.py` shim kept but `_run_nested_validation` name still internal (Low)
- Impact insight (from `nexus impact --json` when available):
  - Direct dependents of `required_folds`/`scale_count_gate`: `discovery.py`, `phase2_support.py`, `config.py` wrappers, `rb_governor.py`
  - Blast radius of `rolling_cv` deletion: `rb_governor.py`, `test_rolling_cv.py`
  - Blast radius of `SPLIT_MODE` removal: `test_data_splitter_properties.py`, `test_phase2_support.py`, `test_phase2_rule_pool.py`, `test_phase5_oos.py`
- Existing patterns to match:
  - Exemplar: `gpu_fuzzy_trader/validation/fold_gates.py` — pure functions `scale_count_gate` with explicit `FoldExposure`, `required_folds` with ceiling, `build_fold_gate_context`/`resolve_fold_gates`
  - Exemplar: `gpu_fuzzy_trader/mtf/cross_fitting.py` — geometry-only `TemporalFold`, `eligible_for_role`, `purge_for_role`

## Findings triage
| # | Finding | Category | Impact | Effort | Confidence | Evidence |
|---|---------|----------|--------|--------|------------|----------|
| H1 | fold-support ratio unused, discovery uses fallback 2 | correctness | HIGH | S | HIGH | `config.py:2225` vs `discovery.py:645` |
| H2 | count-gate scaling not active in Phase 2, base floor returned, legacy rounding off-by-one | correctness | HIGH | M | HIGH | `fold_gates.py:238`, `config.py:2250`, `phase2_support.py:78` |
| M1 | `rolling_cv` import remains, test collection fails | correctness | MEDIUM | S | HIGH | `rb_governor.py:3403`, `test_rolling_cv.py:11` |
| M2 | old tests still require `SPLIT_MODE` | correctness | MEDIUM | S | HIGH | `test_data_splitter_properties.py:114`, `test_phase2_support.py:343` |
| L1 | inline tests vs prod, is_seed compat, _run_nested_validation name | cleanup | LOW | S | HIGH | `cross_fitting.py:49`, Task-1 tests |

Rejected / by-design:
- 9 commits ahead of origin/main — by-design, part of this series; not a defect

## Task breakdown (ordered, dependencies noted)
### Task 1: Use ratio-based fold support in discovery (slug: ratio-support)
- id: task-1
- Effort: S
- Confidence: HIGH
- Risk if wrong: HIGH — too few OOF folds admitted, weak rules pass
- Depends on: none
- Evidence:
  - `gpu_fuzzy_trader/config.py:2225-2236` — `required_folds` helper
  - `gpu_fuzzy_trader/mtf/discovery.py:645-646` — current fallback to 2
  - `gpu_fuzzy_trader/validation/fold_gates.py:163` — `required_folds(eligible, ratio)`
- Scope:
  - In: `gpu_fuzzy_trader/mtf/discovery.py`, `gpu_fuzzy_trader/config.py` (if needed), `tests/unit/test_mtf_composer.py` (maybe)
  - Out: `gpu_fuzzy_trader/phases/phase2_support.py`, `gpu_fuzzy_trader/rb_governor.py`, `tests/property/*`, `gpu_fuzzy_trader/validation/nested_walk_forward.py`
  - Related callers (blast): `discover_directional_layer`, OOF aggregation, composer
- Acceptance criteria:
  - [ ] `discovery.py:645` replaced with `from gpu_fuzzy_trader.config import required_folds` or `from gpu_fuzzy_trader.validation.fold_gates import required_folds` using `config.MTF_MIN_FOLD_SUPPORT_RATIO`; e.g. `min_fold_support = required_folds(len(valid_folds))` or `_cfg.required_folds(len(valid_folds))` where eligible = len(valid_folds)
  - [ ] For 3 eligible folds, required = 3 (not 2); for 4 eligible, required = 3; for 2 eligible, required = 2 (max 2 floor) — verified via probe
  - [ ] No `getattr(_cfg, "MTF_MIN_FOLD_SUPPORT"` fallback remains in discovery
- Verification gates (each task ends with these, exact commands):
  1. `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_mtf_cross_fitting.py tests/unit/test_mtf_composer.py` — expected: pass
  2. `PYTEST_LOW_MEMORY=1 .venv/bin/python -c "from gpu_fuzzy_trader.config import required_folds; assert required_folds(3)==3; assert required_folds(4)==3; assert required_folds(2)==2; print('ratio ok')"` — expected: prints ratio ok
  3. `grep -R "MTF_MIN_FOLD_SUPPORT[^_]" --include="*.py" gpu_fuzzy_trader/` — expected: 0 hits (outside ratio)
- STOP conditions:
  - STOP if `gpu_fuzzy_trader/config.py:2225` does not contain `def required_folds`
  - STOP if `gpu_fuzzy_trader/mtf/discovery.py` does not contain the fallback line at 645
  - STOP if `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_mtf_composer.py` was failing on base before change

### Task 2: Activate production count-gate scaling in Phase 2 (slug: phase2-gate-scaling)
- id: task-2
- Effort: M
- Confidence: HIGH
- Risk if wrong: HIGH — tiny folds not penalized, floors unchanged (120/25 probe)
- Depends on: task-1
- Evidence:
  - `gpu_fuzzy_trader/validation/fold_gates.py:238` — `build_fold_gate_context` no production caller
  - `gpu_fuzzy_trader/config.py:2250` — `if reference_rows is None: return int(base)` retains base floor
  - `gpu_fuzzy_trader/phases/phase2_support.py:78` — uses `effective_min_trade_support(n_rows)` which hits that path; probe 25k rows still returns 120/25
  - `gpu_fuzzy_trader/config.py:2257` — `rounding="legacy"` yields 11 where ceil yields 12
  - `gpu_fuzzy_trader/validation/fold_gates.py:119` — `scale_count_gate` with `rounding` param
- Scope:
  - In: `gpu_fuzzy_trader/phases/phase2_support.py`, `gpu_fuzzy_trader/config.py`, `gpu_fuzzy_trader/validation/fold_gates.py` (if needed for rounding), `tests/unit/test_phase2_support.py` (expectations)
  - Out: `gpu_fuzzy_trader/mtf/discovery.py`, `gpu_fuzzy_trader/rb_governor.py`, `tests/property/test_data_splitter_properties.py`
  - Related callers (blast): `resolve_evolution_floors`, `effective_min_trade_support`, `effective_min_trade_pool_floor`, `effective_pool_min_val_trades`
- Acceptance criteria:
  - [ ] `phase2_support.py:resolve_evolution_floors` (and `_pool_admission_floors`) call `build_fold_gate_context` or `scale_count_gate` with explicit train/OOF reference exposure (e.g. `reference_df` = full train, `scoring_df` = island n_rows or valid rows), not the no-reference path; `n_rows` probe with 25k vs 100k reference yields scaled values (e.g. 120→30, 25→7) not unchanged 120/25
  - [ ] `config.py:scale_trade_floor` when called with `reference_rows` uses `rounding="ceil"` (spec) not legacy, so 40 * 25k/100k = 10 not 11 off-by-one; when `reference_rows is None` it still returns base for backwards compat but Phase 2 no longer hits that path
  - [ ] `fold_gates.py:238` has at least one production caller after this task (phase2_support)
  - [ ] Existing `test_phase2_support.py` updated to expect ceil scaling (or with reference exposure) and passes
- Verification gates:
  1. `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_phase2_support.py` — expected: pass with ceil expectations
  2. `PYTEST_LOW_MEMORY=1 .venv/bin/python -c "from gpu_fuzzy_trader.config import effective_min_trade_support; print(effective_min_trade_support(25000))"` — manual probe shows scaled vs 120 base (illustrative)
  3. `grep -R "build_fold_gate_context|scale_count_gate" --include="*.py" gpu_fuzzy_trader/phases/` — expected: at least 1 hit in phase2_support.py
- STOP conditions:
  - STOP if `gpu_fuzzy_trader/phases/phase2_support.py:resolve_evolution_floors` does not exist or signature changed
  - STOP if `gpu_fuzzy_trader/validation/fold_gates.py` does not contain `def scale_count_gate`
  - STOP if `tests/unit/test_phase2_support.py` was already failing on base (run baseline first)

### Task 3: Remove remaining rolling_cv and SPLIT_MODE test dependencies (slug: legacy-cleanup)
- id: task-3
- Effort: S
- Confidence: HIGH
- Risk if wrong: MEDIUM — import errors break collection / CI
- Depends on: task-2
- Evidence:
  - `gpu_fuzzy_trader/rb_governor.py:3403` — `from gpu_fuzzy_trader.validation.rolling_cv import cv_folds_only`
  - `tests/unit/test_rolling_cv.py:11` — `from gpu_fuzzy_trader.validation.rolling_cv import …` ModuleNotFoundError
  - `tests/property/test_data_splitter_properties.py:114` — `original_mode = config_mod.SPLIT_MODE` AttributeError
  - `tests/unit/test_phase2_support.py:343` — `monkeypatch.setattr(_cfg, "SPLIT_MODE", "purged_walk_forward")` AttributeError
  - `gpu_fuzzy_trader/config.py` — `SPLIT_MODE` already removed, but tests still patch it
- Scope:
  - In: `gpu_fuzzy_trader/rb_governor.py`, `tests/unit/test_rolling_cv.py` (delete or rewrite to fold_gates), `tests/property/test_data_splitter_properties.py`, `tests/unit/test_phase2_support.py`, `tests/unit/test_phase2_rule_pool.py`, `tests/unit/test_phase5_oos.py` (where SPLIT_MODE appears)
  - Out: `gpu_fuzzy_trader/mtf/discovery.py`, `gpu_fuzzy_trader/mtf/cross_fitting.py`, `gpu_fuzzy_trader/validation/fold_gates.py`
  - Related callers (blast): `test_rolling_cv`, `test_data_splitter_properties`, `test_phase2_support`, `test_phase2_rule_pool`, `test_phase5_oos`
- Acceptance criteria:
  - [ ] `gpu_fuzzy_trader/rb_governor.py:3403` no longer imports `rolling_cv`; uses `cv_folds` directly with `getattr(fold, "is_holdout", False)` already present or equivalent safe filter
  - [ ] `tests/unit/test_rolling_cv.py` either deleted or rewritten to assert `validation.fold_gates` / `mtf.cross_fitting` (no `rolling_cv` import) and `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_rolling_cv.py` passes or file is removed and collection succeeds
  - [ ] `tests/property/test_data_splitter_properties.py:114` updated to not reference `SPLIT_MODE` (use `HOLDOUT_TRAIN_FRACTION` or remove monkeypatch) and `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/property/test_data_splitter_properties.py -k test_property_5` passes
  - [ ] `tests/unit/test_phase2_support.py:343` and similar `SPLIT_MODE` patches removed/updated and `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_phase2_support.py tests/unit/test_phase2_rule_pool.py tests/unit/test_phase5_oos.py` collection succeeds (no AttributeError)
- Verification gates:
  1. `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest --collect-only tests/unit/test_rolling_cv.py 2>&1 | tail -n 20` — expected: no ModuleNotFoundError, collection succeeds
  2. `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/property/test_data_splitter_properties.py tests/unit/test_phase2_support.py tests/unit/test_phase2_rule_pool.py 2>&1 | tail -n 20` — expected: pass / no AttributeError
  3. `grep -R "rolling_cv|SPLIT_MODE" --include="*.py" gpu_fuzzy_trader/ tests/ | grep -v ".pyc" | grep -v "__pycache__"` — expected: 0 hits in prod, 0 in tests (outside docs/allowlist)
- STOP conditions:
  - STOP if `gpu_fuzzy_trader/rb_governor.py:3403` does not contain `rolling_cv` import as assumed
  - STOP if `tests/unit/test_rolling_cv.py` does not exist (already deleted) — treat as done
  - STOP if `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest --collect-only` was failing on base for other reasons (run baseline first)

### Task 4: Low-priority cleanup and coverage (slug: low-cleanup)
- id: task-4
- Effort: S
- Confidence: MEDIUM
- Risk if wrong: LOW — cosmetic but ensures production coverage
- Depends on: task-3
- Evidence:
  - `gpu_fuzzy_trader/mtf/cross_fitting.py:49` — `is_seed` compatibility property remains
  - `tests/unit/test_unified_folds.py` etc — still test inline formulas rather than production `fold_gates.required_folds`/`scale_count_gate`
  - `gpu_fuzzy_trader/validation/nested_walk_forward.py` shim keeps `_run_nested_validation` internal name
  - 114 focused tests passed, but Task-1 inline tests not asserting prod module
- Scope:
  - In: `gpu_fuzzy_trader/mtf/cross_fitting.py`, `tests/unit/test_unified_folds.py`, `tests/unit/test_fold_gates.py`, `tests/unit/test_purge_leakage.py`, `gpu_fuzzy_trader/validation/nested_walk_forward.py` / `walk_forward_stability_report.py`
  - Out: `gpu_fuzzy_trader/phases/phase2_support.py`, `gpu_fuzzy_trader/rb_governor.py`, `gpu_fuzzy_trader/config.py` (unless needed for alias removal)
  - Related callers (blast): `cross_fitting` is_seed callers, nested validation callers
- Acceptance criteria:
  - [ ] `gpu_fuzzy_trader/mtf/cross_fitting.py:49` `is_seed` compat removed or kept as deprecated alias with `warnings.warn` but not used internally; `eligible_for_role` is the single implementation path; `grep -R "is_seed" --include="*.py" gpu_fuzzy_trader/` returns 0 or only deprecated shim
  - [ ] `tests/unit/test_unified_folds.py` and `test_fold_gates.py` updated to import and assert `validation.fold_gates` production helpers (`scale_count_gate`, `required_folds`, `build_fold_gate_context`) in addition to geometry checks — at least one test per file asserts prod module, not just inline formula
  - [ ] `gpu_fuzzy_trader/validation/nested_walk_forward.py` internal `_run_nested_validation` renamed to `run_stability_report` or removed if shim, and no dangling internal name remains
  - [ ] `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_unified_folds.py tests/unit/test_fold_gates.py tests/unit/test_purge_leakage.py` — expected: pass with prod assertions
- Verification gates:
  1. `grep -R "is_seed" --include="*.py" gpu_fuzzy_trader/` — expected: 0 or only `warnings.warn` shim
  2. `grep -R "_run_nested_validation" --include="*.py" gpu_fuzzy_trader/` — expected: 0
  3. `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_unified_folds.py tests/unit/test_fold_gates.py tests/unit/test_purge_leakage.py` — expected: pass
- STOP conditions:
  - STOP if `gpu_fuzzy_trader/mtf/cross_fitting.py:49` does not contain `is_seed` as assumed
  - STOP if Task-1 tests already assert production module (no change needed) — treat as done
  - STOP if `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_mtf_cross_fitting.py` was failing on base

## Execution order & dependency graph
- Recommended order: task-1 → task-2 → task-3 → task-4
- Parallelizable: task-1 and task-2 share config but task-2 needs task-1's ratio helper, so sequential
- Mermaid:
```mermaid
flowchart LR
  t1[task-1 ratio-support] --> t2[task-2 phase2-gate-scaling]
  t2 --> t3[task-3 legacy-cleanup]
  t3 --> t4[task-4 low-cleanup]
```

## Verification strategy (global)
- Baseline: `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q tests/unit/test_mtf_cross_fitting.py tests/unit/test_mtf_composer.py tests/unit/test_data_splitter.py tests/unit/test_fold_gates.py tests/unit/test_purge_leakage.py tests/unit/test_phase2_support.py 2>&1 | tail -n 20` on base before each task
- Per-task: run task-specific gates above
- Final: `PYTEST_LOW_MEMORY=1 .venv/bin/python -m pytest -q --ignore=tests/benchmark --ignore=tests/unit/test_gpu_engine.py --ignore=tests/unit/test_gpu_rule_set_batch.py` (focused, not full suite) and ensure 0 `rolling_cv`/`SPLIT_MODE` hits

## Rollback / safety
- Each task is a feature branch worktree isolated; discard if blocked
- No forced push to base without user confirmation

## Outcome memory
- After each task completes, orchestrator records outcomes in `.opencode/handoffs/` + `LESSONS.md`
